{
  "scripts": {
    "start": "sudo setcap 'cap_net_bind_service=+ep' `which node` ;nodemon node_modules/meshcentral --cert localhost --port $Port --redirport 443"
  },
  "dependencies": {
    "express": "^4.21.2",
    "meshcentral": "^1.1.49",
    "modern-syslog": "1.2.0",
    "otplib": "12.0.1",
    "syslog": "0.1.1-1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
